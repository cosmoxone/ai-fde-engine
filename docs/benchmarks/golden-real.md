# 真实 LLM 黄金集评估报告（占位）

> **状态：待生成** —— 本报告需要真实 LLM API Key（DeepSeek/Qwen，20 条样本约消耗几元额度）。
> Key 配置后执行以下命令即自动生成，本文件将被真实数据替换：

```bash
# 1. 配置 Key（任选其一）
export DEEPSEEK_API_KEY=sk-...
# 或启动后在 Dashboard 设置页填写（热生效）

# 2. 生成真实报告
python -m src.evaluation.golden           # 黄金集要点命中率（目标 ≥80%）
python -m src.evaluation.benchmark_report # 基线报告（会标注 real-llm 模式）
```

## 报告模板（生成后填充）

### 1. 黄金集要点命中率（真实 LLM）

| 行业 | 样本数 | 要点命中率 | 判定 |
| --- | --- | --- | --- |
| 制造业 | 8 | 待测 | ≥80% 目标 |
| 金融业 | 6 | 待测 | ≥80% 目标 |
| 政务 | 6 | 待测 | ≥80% 目标 |
| **总体** | 20 | 待测 | |

### 2. 未命中要点清单（改进依据）

（生成后填充）

### 3. 结论

- 达标（≥80%）→ 可对外宣传质量主张
- 未达标 → 如实披露 + 列出改进项，不做质量宣传（见 docs/09 决策原则）

---

*验证前置的自我要求：本项目的质量主张必须先被自己的黄金集验证。*
