# 安全策略

## 支持版本

| 版本 | 支持状态 |
| ---- | -------- |
| 1.0.x | ✅ 支持 |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。**

请通过以下方式私下报告：

1. 使用 GitHub 私有安全通告（Security Advisories → Report a vulnerability）
2. 或发送邮件至维护者邮箱（请在报告中注明项目名与版本）

我们承诺：

- 48 小时内确认收到报告
- 7 天内给出初步评估（严重等级与影响范围）
- 修复方案确定后，在高危漏洞修复发布前不公开细节
- 在更新日志与安全通告中致谢报告者（除非要求匿名）

## 安全设计基线

项目在架构层面的安全要求（详见 `docs/01-需求文档.md` §4.3 与 `docs/02-技术方案.md` §5）：

- 客户数据加密存储（AES-256）、传输加密（TLS 1.3）
- 代码 Agent 沙箱隔离（Docker 容器、只读挂载、网络受限）
- 敏感信息自动脱敏（API Key、手机号、身份证等）
- 关键节点人工审核兜底，AI 输出不直接上线
- 操作审计日志可追溯，保留期 ≥ 180 天

## ⚠️ 当前 MVP 安全边界（重要）

以下能力**尚未实现**，生产部署前必须评估：

- ❌ API 认证与 RBAC：当前 REST API 无认证，**仅可在受信任内网运行**
- ❌ 数据持久化加密：业务数据为内存态
- ❌ 多租户强制隔离：客户隔离依赖部署层面的网络隔离

在上述能力落地前，请勿将本系统直接暴露于公网，请勿在生产环境处理未脱敏的客户数据。
